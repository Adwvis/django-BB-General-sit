name: Build and Publish image to Docker Hub

on:
  [workflow_dispatch]

jobs:
  publish_image:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: build image
        run: docker build -t Adwvis/django-BB-General-sit:latest .
      - name: push image to docker hub
        run: |
          docker login -u Adwvis -p ${{ secrets.DOCKER_HUB_TOKEN}}
          docker push Adwvis/django-BB-General-sit:latest 

      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Build and push image
      #   run: |
      #     docker build -t YOUR_DOCKERHUB_USERNAME/YOUR_IMAGE_NAME:latest .
      #     docker push YOUR_DOCKERHUB_USERNAME/YOUR_IMAGE_NAME:latest